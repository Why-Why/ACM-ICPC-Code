1,双连通分量那里init里面给Belong清零。
2,割点里面init给Bridge清零。
3,网络流里面栈优化版本的dis[0]=N那一行要去掉？
4,树状数组有更新。
5,求固定区间大小，位置不固定的问题，可以把点拆成两个(x,a)和(x+w,-a)。
6,vim配置set timeout timeoutlen=3000 ttimeoutlen=100。
7,环形最大字段和，可以记录最小字段和来解决。
8,树状数组更新区间问题：更新区间(a,b)可以在a出加上d，b+1出减去d，不过这样维护的就不是前缀和而是某个点的值(仍通过sum求)。
9,一维RMQ那里汉字注释有问题，然后加上一句注意((i&(i-1))==0)的括号问题。Treap那里汉字也有问题。
10,LCA模板不能运行。
11,Dijkstra优化，如果每个点向外的边长相同，可以在优先队列排序的时候再添加一个边长来优化。
12,Treap中的ch是二维数组可能会慢，可以改为一维的。
13,Manacher算法中l改成大写，然后注意里面加一句 char注意改成int！！！
14,高精度增加几个注意：最低位在数组0位置。scan函数数组可能爆栈。>变成>=只需要在最后那个return 0变成return 1。
15,曼哈顿最小生成树要写，斜率优化DP和四边形不等式要写，lucas定理。
16,CDQ分治，对时间分治，整体二分。
17,bitset的应用。
18,计算几何InPoly这个函数有更改，OnSeg有修改。
19,树链剖分有更改。
20,SA有更改。
